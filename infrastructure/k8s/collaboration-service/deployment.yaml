apiVersion: apps/v1
kind: Deployment
metadata:
  name: collaboration-service
  namespace: collabcode
spec:
  replicas: 1
  selector:
    matchLabels:
      app: collaboration-service
  template:
    metadata:
      labels:
        app: collaboration-service
    spec:
      containers:
        - name: collaboration-service
          image: collabcode/collaboration-service:latest
          ports:
            - containerPort: 4000
