/** Default file lock TTL in seconds. */
export const LOCK_TTL_SECONDS = 30;

/** Client lock heartbeat interval in seconds. */
export const LOCK_RENEW_INTERVAL_SECONDS = 15;

/** Maximum allowed execution runtime in milliseconds. */
export const MAX_EXECUTION_TIMEOUT_MS = 10_000;

/** Max execution memory limit in megabytes. */
export const MAX_EXECUTION_MEMORY_MB = 128;

/** Max execution CPU limit in cores. */
export const MAX_EXECUTION_CPU_CORES = 0.5;
