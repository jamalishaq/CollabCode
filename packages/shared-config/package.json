{
  "name": "@collabcode/shared-config",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -w -p tsconfig.json",
    "test": "echo \"No tests yet\"",
    "lint": "eslint src --ext .ts"
  }
}
