# auth-service

Handles user registration, login, and token issuance workflows.

## Endpoints

- `GET /health`

## Setup

1. Copy `.env.example` to `.env`.
2. Install dependencies with `pnpm install`.
3. Start locally using `pnpm run dev`.
