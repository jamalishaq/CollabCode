# notification-service

Consumes async events and dispatches user notifications.

## Endpoints

- `GET /health`

## Setup

1. Copy `.env.example` to `.env`.
2. Install dependencies with `npm install`.
3. Start locally using `npm run dev`.
