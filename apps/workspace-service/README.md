# workspace-service

Handles workspace and project CRUD plus membership and role management.

## Endpoints

- `GET /health`

## Setup

1. Copy `.env.example` to `.env`.
2. Install dependencies with `pnpm install`.
3. Start locally using `pnpm run dev`.
